enum AuthStatus {
  unknown,
  authenticated,
  unauthenticated,
}

enum SignInStatus {
  initial,
  submitting,
  success,
  error,
}

enum SignUpStatus {
  initial,
  submitting,
  success,
  error,
}

enum KidsStepType {
  index0,
  index1,
  index2,
}
